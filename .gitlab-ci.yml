stages:
    - test
    - build
    - deploy

before_script:
    - 'which ssh-agent'
    - 'eval $(ssh-agent -s)'
    - 'echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'

# ----------------------------------- AMBIENTES DE TESTES -------------------------------------

teste-cmbhecid:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmbhecid "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@cmbhecid "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-cmarinos:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmarinos.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@cmarinos.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-cmbocaiuva:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmbocaiuva.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@cmbocaiuva.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-cmcapitaoeneas:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmcapitaoeneas "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@cmcapitaoeneas "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-cmmoc:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no ecidade@cmmoc.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 ecidade@cmmoc.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-cmnavarro:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.251.27.4 "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@10.251.27.4 "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-cmuba:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmuba.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@cmuba.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-e-cidade:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@e-cidade.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git checkout . && git pull origin stable "'
        # - 'ssh -p22 contass@e-cidade.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmpirapora:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmvarzelandia:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmvarzelandia.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmvarzelandia.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmverdelandia:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmverdelandia.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmverdelandia.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmibiracatu:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmibiracatu.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmibiracatu.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmsaojoaodaponte:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmsaojoaodaponte.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmsaojoaodaponte.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-prevmoc:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no ecidade@prevmoc.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 ecidade@prevmoc.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmsaofrancisco:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmsaofrancisco.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmsaofrancisco.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmclarodospocoes:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmclarodospocoes.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmclarodospocoes.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-mctrans:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no ecidade@10.251.27.64 "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 ecidade@10.251.27.64 "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-prefeituras:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@prefeituras.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@prefeituras.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-cmpirapora:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@cmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmlagoadospatos:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmlagoadospatos.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmlagoadospatos.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-prefeituras2:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.250.30.24 "cd /var/www/e-cidade && git pull origin stable "'
        # - 'ssh -p22 contass@10.250.30.24 "cd /var/www/e-cidade && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmsaoromao:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmsaoromao.contassconsultoria.com.br "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@pmsaoromao.contassconsultoria.com.br "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmnovaporteirinha:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmnovaporteirinha.contassconsultoria.com.br "cd /var/www/e-cidade && git pull origin stable "'
        # - 'ssh -p22 contass@pmnovaporteirinha.contassconsultoria.com.br "cd /var/www/e-cidade && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmburitizeiro:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.251.27.95 "cd /var/www/e-cidade && git pull origin stable "'
        # - 'ssh -p22 contass@10.251.27.95 "cd /var/www/e-cidade && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmubai:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmubai "cd /var/www/e-cidade && git pull origin stable "'
        # - 'ssh -p22 contass@pmubai "cd /var/www/e-cidade && php deploy_git.php "'
    environment: development
    only:
        - stable

teste-pmgraomogol:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.251.27.96 "cd /var/www/e-cidade && git pull origin stable "'
        # - 'ssh -p22 contass@10.251.27.96 "cd /var/www/e-cidade && php deploy_git.php "'
    environment: development
    only:
        - stable

teste:
    stage: test
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@teste "cd /var/www/e-cidade-teste && git fetch && git checkout -t origin/stable && git pull origin stable "'
        # - 'ssh -p22 contass@teste "cd /var/www/e-cidade-teste && php deploy_git.php "'
    environment: development
    only:
        - stable

# ----------------------------------- AMBIENTES DE PRODUCAO -------------------------------------

cmbhecid:
    stage: deploy
    script:
        - 'ssh-add <(echo "$STAGING_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmbhecid "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@cmbhecid "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

cmarinos:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmarinos.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@cmarinos.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

cmbocaiuva:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmbocaiuva.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@cmbocaiuva.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

cmcapitaoeneas:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmcapitaoeneas "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@cmcapitaoeneas "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

cmmoc:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no ecidade@cmmoc.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 ecidade@cmmoc.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

cmnavarro:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.251.27.4 "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@10.251.27.4 "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

cmuba:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmuba.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@cmuba.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

# cisaje:
#     stage: deploy
#     script:
#         - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
#         - 'ssh -o StrictHostKeyChecking=no contass@cisaje.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
#         - 'ssh -p22 contass@cisaje.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
#     environment: production
#     only:
#         - master

# ciiamaje:
#     stage: deploy
#     script:
#         - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
#         - 'ssh -o StrictHostKeyChecking=no contass@ciiamaje.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
#         - 'ssh -p22 contass@ciiamaje.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
#     environment: production
#     only:
#         - master

e-cidade:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@e-cidade.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@e-cidade.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

pmpirapora:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

pmvarzelandia:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmvarzelandia.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmvarzelandia.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

pmverdelandia:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmverdelandia.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmverdelandia.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

# pmluislandia:
#     stage: deploy
#     script:
#         - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
#         - 'ssh -o StrictHostKeyChecking=no contass@pmluislandia.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
#         - 'ssh -p22 contass@pmluislandia.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
#     environment: production
#     only:
#         - master

# pmcristalia:
#     stage: deploy
#     script:
#         - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
#         - 'ssh -o StrictHostKeyChecking=no contass@pmcristalia.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
#         - 'ssh -p22 contass@pmcristalia.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
#     environment: production
#     only:
#         - master

pmibiracatu:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmibiracatu.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmibiracatu.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

# pmitacambira:
#     stage: deploy
#     script:
#         - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
#         - 'ssh -o StrictHostKeyChecking=no contass@pmitacambira.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
#         - 'ssh -p22 contass@pmitacambira.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
#     environment: production
#     only:
#         - master

pmsaojoaodaponte:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmsaojoaodaponte.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmsaojoaodaponte.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

prevmoc:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no ecidade@prevmoc.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 ecidade@prevmoc.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

# samuecid:
#     stage: deploy
#     script:
#         - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
#         - 'ssh -o StrictHostKeyChecking=no contass@samuecid.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
#         - 'ssh -p22 contass@samuecid.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
#     environment: production
#     only:
#         - master

pmsaofrancisco:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmsaofrancisco.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmsaofrancisco.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

pmclarodospocoes:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmclarodospocoes.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmclarodospocoes.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

mctrans:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no ecidade@10.251.27.64 "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 ecidade@10.251.27.64 "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

prefeituras:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@prefeituras.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@prefeituras.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

cmpirapora:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@cmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@cmpirapora.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

pmlagoadospatos:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmlagoadospatos.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmlagoadospatos.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

prefeituras2:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.250.30.24 "cd /var/www/e-cidade && git pull origin master "'
        - 'ssh -p22 contass@10.250.30.24 "cd /var/www/e-cidade && php deploy_git.php "'
    environment: production
    only:
        - master

pmsaoromao:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmsaoromao.contassconsultoria.com.br "cd /var/www/e-cidade-git && git pull origin master "'
        - 'ssh -p22 contass@pmsaoromao.contassconsultoria.com.br "cd /var/www/e-cidade-git && php deploy_git.php "'
    environment: production
    only:
        - master

pmnovaporteirinha:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmnovaporteirinha.contassconsultoria.com.br "cd /var/www/e-cidade && git pull origin master "'
        - 'ssh -p22 contass@pmnovaporteirinha.contassconsultoria.com.br "cd /var/www/e-cidade && php deploy_git.php "'
    environment: production
    only:
        - master

pmburitizeiro:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.251.27.95 "cd /var/www/e-cidade && git pull origin master "'
        - 'ssh -p22 contass@10.251.27.95 "cd /var/www/e-cidade && php deploy_git.php "'
    environment: production
    only:
        - master

pmubai:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@pmubai "cd /var/www/e-cidade && git pull origin master "'
        - 'ssh -p22 contass@pmubai "cd /var/www/e-cidade && php deploy_git.php "'
    environment: production
    only:
        - master

pmgraomogol:
    stage: deploy
    script:
        - 'ssh-add <(echo "$PRODUCTION_PRIVATE_KEY")'
        - 'ssh -o StrictHostKeyChecking=no contass@10.251.27.96 "cd /var/www/e-cidade && git pull origin master "'
        - 'ssh -p22 contass@10.251.27.96 "cd /var/www/e-cidade && php deploy_git.php "'
    environment: production
    only:
        - master
